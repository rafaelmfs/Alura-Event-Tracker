//Essa é a interface do objeto que contém as informações de filtro, foi somente adicionado mais um atributo que é o estado.
export interface IFiltroEventos{
    data?: Date | null
    estado?: string
}